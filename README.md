# Inicializácia a prvý commit cez repozitár priamo z CLI
git init
git add README.md
git commit -m "Initial commit - GitHub Pilot"
git branch -M main
git remote add origin <URL_TVOJHO_REPOZITÁRA>
git push -u origin main
# GitHub Pilot
